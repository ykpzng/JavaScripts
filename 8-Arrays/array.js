//Arrays

let arrayList = ['Ali', 'Veli', 'Hasan', 23, 54];
console.log(arrayList);

for (let i = 0; i < arrayList.length; i++) {
  console.log(arrayList[i]);
}

arrayList.forEach(element => {
  console.log(element);
});


